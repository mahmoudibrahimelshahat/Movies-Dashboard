import { SliceDescPipe } from './slice-desc.pipe';

describe('SliceDescPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceDescPipe();
    expect(pipe).toBeTruthy();
  });
});
